name: debt_manager_app
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.18.0
  sqflite: ^2.3.0
  path_provider: ^2.1.1
  equatable: ^2.0.5
  flutter_bloc: ^8.1.3 # أو أي حزمة إدارة حالة أخرى مثل provider
  # google_sign_in: # لإضافتها مستقبلاً لمزامنة Google Drive
  # googleapis: # لإضافتها مستقبلاً لمزامنة Google Drive

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  generate: true # لتفعيل إنشاء ملفات الترجمة

  # assets:
  #   - images/a_dot_burr.jpeg

  # fonts:
  #   - family: NotoSansArabic
  #     fonts:
  #       - asset: fonts/NotoSansArabic-Regular.ttf
  #       - asset: fonts/NotoSansArabic-Bold.ttf
  #         weight: 700
